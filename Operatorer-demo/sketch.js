
function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
circle(mouseX, mouseY, 50);
if (mouseX > 200 && mouseY < 200)
fill("red")


function draw()
  background(220);
circle(mouseX, mouseY, 50);
if (mouseX < 200 && mouseY > 200)
  fill("red")


function draw() 
  background(220);
circle(mouseX, mouseY, 50);
if (mouseX > 200 && mouseY > 200)
  fill("white")



function draw()
  background(220);
circle(mouseX, mouseY, 50);
if (mouseX < 200 && mouseY < 200)
  fill("white")
}
